export interface Post {
    _id: string;
    title: string;
    description: string;
    image?: string; // Optional image URL
    content?: string;
    createdAt?: string;
    updatedAt?: string;
  }
  